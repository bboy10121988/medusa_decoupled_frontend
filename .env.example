# ===========================================
# Medusa 後端 API 設定
# ===========================================
# 環境切換（local | vm）
NEXT_PUBLIC_ENV_MODE=請填入 local 或 vm

# 本機環境 URLs
MEDUSA_BACKEND_URL_LOCAL=http://localhost:9000
NEXT_PUBLIC_MEDUSA_BACKEND_URL_LOCAL=前端可見的本機 Medusa 後端 URL（如：http://localhost:9000）
NEXT_PUBLIC_MEDUSA_ADMIN_URL_LOCAL=本機 Medusa Admin URL（如：http://localhost:9000）
NEXT_PUBLIC_MEDIA_URL_LOCAL=本機媒體服務 URL（如：http://localhost:9000）

# VM 環境 URLs（請替換成你的 VM IP 或網域）
MEDUSA_BACKEND_URL_VM=http://<your-vm-host-or-domain>:9000
NEXT_PUBLIC_MEDUSA_BACKEND_URL_VM=前端可見的 VM Medusa 後端 URL（如：http://<vm>:9000）
NEXT_PUBLIC_MEDUSA_ADMIN_URL_VM=VM Medusa Admin URL（如：http://<vm>:9000）
NEXT_PUBLIC_MEDIA_URL_VM=VM 媒體服務 URL（如：http://<vm>:9000）

# 當前使用的 URLs（可依 ENV_MODE 切換指向）
MEDUSA_BACKEND_URL=實際使用的 Medusa 後端 URL
NEXT_PUBLIC_MEDUSA_BACKEND_URL=實際使用的前端可見後端 URL
NEXT_PUBLIC_MEDUSA_ADMIN_URL=實際使用的 Admin URL

# ===========================================
# API 認證（Publishable Key）
# ===========================================
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY_LOCAL=本機 Publishable Key（從後端取得）
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY_VM=VM Publishable Key（從後端取得）
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=實際使用的 Publishable Key（依 ENV_MODE 選擇）

# ===========================================
# 環境設定
# ===========================================
NODE_ENV=development
NEXT_PUBLIC_BASE_URL=前端網站基底 URL（如：http://localhost:8001）

# ===========================================
# 區域與貨幣設定
# ===========================================
NEXT_PUBLIC_DEFAULT_REGION=預設地區代碼（如：tw）
NEXT_PUBLIC_DEFAULT_CURRENCY=預設貨幣（如：TWD）
NEXT_PUBLIC_DEFAULT_LANGUAGE=預設語言（如：zh-TW）

# 地區映射設定（用於後端 Region ID）
NEXT_PUBLIC_REGION_TW=台灣地區的 Region ID（從後端取得）
NEXT_PUBLIC_REGION_EU=歐洲地區的 Region ID（從後端取得）

# ===========================================
# Sanity CMS 設定
# ===========================================
NEXT_PUBLIC_SANITY_PROJECT_ID=Sanity 專案 ID
NEXT_PUBLIC_SANITY_DATASET=Sanity Dataset 名稱（如：production）
NEXT_PUBLIC_SANITY_API_VERSION=Sanity API 版本（如：2024-01-01）

# ===========================================
# 網站資訊
# ===========================================
NEXT_PUBLIC_SITE_URL=網站公開 URL（SEO、連結用）
NEXT_PUBLIC_STORE_NAME=商店名稱（顯示於前端）

# ===========================================
# 圖片與媒體
# ===========================================
NEXT_PUBLIC_MEDIA_URL=媒體服務 URL（用於圖片等靜態資源）

# ===========================================
# Google 身分驗證設定
# ===========================================

# 選項 1: Medusa Google OAuth (後端處理，傳統方式)
# 請將以下設定配置到後端 Medusa 的 .env 檔案：
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret  
# GOOGLE_CALLBACK_URL=https://yourstore.com/auth/google/callback

# 選項 2: Google Identity Services - GIS (前端直接處理，推薦)
# 從 Google Cloud Console 獲取 Client ID: https://console.cloud.google.com/apis/credentials
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com

# 說明：
# - Medusa OAuth: 使用標準 OAuth 2.0 flow，需要複雜的後端配置
# - Google Identity Services: 使用新版 Google 登入 API，更簡潔安全，推薦使用

# ===========================================
# CORS 來源
# ===========================================
NEXT_PUBLIC_STORE_URL=前端 Storefront URL（允許的來源）
NEXT_PUBLIC_ADMIN_URL=後端 Admin URL（允許的來源）

# ===========================================
# 功能開關
# ===========================================
NEXT_PUBLIC_ENABLE_ANALYTICS=true 或 false（是否啟用分析）
NEXT_PUBLIC_ENABLE_SEARCH=true 或 false（是否啟用搜尋）
NEXT_PUBLIC_ENABLE_WISHLIST=true 或 false（是否啟用願望清單）
FEATURE_SEARCH_ENABLED=true 或 false（後端搜尋功能開關）

# ===========================================
# Webhook 設定
# ===========================================
WEBHOOK_URL=Sanity 等外部系統呼叫的 Webhook 接收 URL
WEBHOOK_SECRET=Webhook 驗證密鑰（自行設定並於後端驗證）

# ===========================================
# 認證設定（NextAuth）
# ===========================================
NEXTAUTH_URL=NextAuth 的站點 URL（如：http://localhost:8001）

# ===========================================
# Sanity Studio 設定
# ===========================================
SANITY_STUDIO_DISABLE_PREVIEW=true 或 false（是否停用預覽）
SANITY_STUDIO_DISABLE_LIVE_EDIT=true 或 false（是否停用即時編輯）

# ===========================================
# 付款方式設定, 對應medusa backend的 payment provider id
# ===========================================
NEXT_PUBLIC_PAYMENT_METHOD_DEFAULT=預設付款方式（如：pp_system_default）
NEXT_PUBLIC_PAYMENT_METHOD_ECPAY_CREDIT=綠界金流信用戶(如：pp_ecpay_credit_card_ecpay_credit_card)


# ===========================================
# 綠界金流（ECPay）設定
# ===========================================
# API_URL
NEXT_PUBLIC_ECPAY_ACTION_URL=https://payment-stage.ecpay.com.tw/Cashier/AioCheckOut/V5
# 特店代號
NEXT_PUBLIC_ECPAY_MERCHANT_ID={特店代號}
# Hash key
NEXT_PUBLIC_ECPAY_HASH_KEY={Hash_Key}
# Hash IV
NEXT_PUBLIC_ECPAY_HASH_IV={Hash_IV}
# 綠界金流CallBack網址
NEXT_PUBLIC_ECPAY_RETURN_URL={對應medusa ecpay 轉換器的網址}