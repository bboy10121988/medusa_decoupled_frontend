// Sanity API re-exports for backward compatibility
export { getHeader, getFooter } from '../sanity'
export { default as client } from '../sanity'
export type { SanityHeader, NavigationItem, SanityImage, Footer } from '../../types/global'
